---
id: REQ-050
title: HealthKit integration for accurate steps/distance
route: C
status: completed
claimed_at: 2026-03-28T10:00:00Z
completed_at: 2026-03-28T16:45:00Z
priority: high
---

# HealthKit Integration

## Goal
Replace GPS-estimated steps and distance with HealthKit data when the user grants permission. Fall back gracefully to current GPS estimates if denied.

## Requirements
- Add HealthKit capability in Xcode (entitlements + project.yml)
- Create `HealthService` (@Observable) that requests read access to `stepCount` and `distanceWalkingRunning`
- Query HealthKit for session-scoped data (Stray session start -> now) for the HUD
- Query HealthKit for daily totals for Stats/timeline views
- LocationService keeps its GPS distance tracking as fallback — HealthKit is display-only
- Cell revealing still uses GPS position (unchanged)
- Add HealthKit permission page to onboarding flow (after location, before photos)
- Permission prompt: explain it's optional, GPS fallback works fine
- Add `NSHealthShareUsageDescription` to Info.plist
- Display unit preference: show HealthKit values when authorized, GPS estimates when not
- Session summary card uses HealthKit values when available

## Notes
- HealthKit is NOT available on iPad — guard with `HKHealthStore.isHealthDataAvailable()`
- Query with `HKStatisticsQuery` for totals, not sample queries
- HealthKit queries are async but fast for small date ranges

---

## Triage

**Route: C** - Complex

**Reasoning:** New external framework integration (HealthKit) spanning multiple layers — new service, entitlements, Info.plist, onboarding flow changes, HUD display changes, stats view changes, and graceful fallback logic. Touches 5+ files across the codebase.

## Plan

**Discovery: HealthKit integration is ~85% complete.** HealthService, onboarding page, Info.plist, HUD display, session summary, and environment injection all exist. Remaining work:

1. **Entitlements** — Populate `Stray/Stray.entitlements` with `com.apple.developer.healthkit` capability (currently empty `<dict/>`)
2. **Settings health section** — Add health authorization toggle to SettingsView (users who skipped onboarding need a path to enable)
3. **Timeline HealthKit overlay** — Query HealthKit for per-day steps/distance in TimelineOverlayView when scrubbing timeline
4. **Persist health values on sessions** — Add `healthStepCount`/`healthDistanceMeters` to StraySession model, pass through on session end, display in StatsView sessions list
5. **Edge case polish** — Guard against mid-session revocation returning 0 when GPS data exists

*Generated by Plan agent*

## Plan Verification

**Source**: REQ-050 (11 items enumerated)
**Pre-fix coverage**: 100% (11/11 items addressed)

### Coverage Map

| # | Requirement | Plan Step | Status |
|---|------------|-----------|--------|
| 1 | Add HealthKit capability (entitlements) | Step 1: Entitlements | Full |
| 2 | Create HealthService (@Observable) | Already exists | Full |
| 3 | Session-scoped data for HUD | Already exists (startHealthRefresh) | Full |
| 4 | Daily totals for Stats/timeline | Step 3: Timeline overlay | Full |
| 5 | GPS fallback (display-only) | Already exists (healthX ?? gpsX pattern) | Full |
| 6 | Cell revealing unchanged | Not touched | Full |
| 7 | Onboarding HealthKit page | Already exists | Full |
| 8 | Permission prompt (optional, GPS fallback) | Already exists | Full |
| 9 | NSHealthShareUsageDescription | Already in Info.plist | Full |
| 10 | Display unit preference | Already exists + Step 3 (timeline) | Full |
| 11 | Session summary card | Already exists + Step 4 (persistence) | Full |

### Fixes Applied

None needed — plan covers all requirements.

*Verified by verify-plan action*

## Exploration

**Not needed** -- plan names specific files and changes from prior session's full codebase exploration. No discovery required.

*Skipped by work action*

## Implementation Summary

- Fixed `StraySession.healthStepCount` and `healthDistanceMeters` from non-optional (0 defaults) to `Optional` (`Int? = nil`, `Double? = nil`) — enables proper `healthValue ?? gpsValue` fallback pattern
- Updated `PersistenceService.saveStraySession` parameters to match optional types
- Updated `StraySessionViewModel` to pass `nil` through instead of coercing to 0
- Cleaned up `StatsView` session list: replaced force-unwrap pattern with clean `session.healthDistanceMeters ?? session.distanceMeters`
- Entitlements, Settings health section, timeline overlay, and revocation guard were already implemented by a parallel process

*Completed by work action (Route C)*

## Testing

**Tests run:** N/A
**Result:** No unit test infrastructure (iOS app, manual device testing per CLAUDE.md). Build verified via `xcodegen generate && xcodebuild build` on Simulator.

*Verified by work action*
