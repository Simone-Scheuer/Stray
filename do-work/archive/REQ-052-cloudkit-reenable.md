---
id: REQ-052
title: Re-enable CloudKit sync
route: B
status: completed
priority: medium
depends_on: Apple Developer Program enrollment
claimed_at: 2026-03-28T17:20:00Z
completed_at: 2026-03-28T17:25:00Z
---

# Re-enable CloudKit Sync

## Goal
With the paid developer account, re-enable iCloud sync so beta testers get multi-device support.

## Steps
- Add CloudKit entitlement back to project
- Update `ModelContainer` config: `cloudKitDatabase: .automatic`
- Verify deduplication logic still works (merge on `cellKey` at launch)
- Test: reveal cells on device A, wait, check device B
- Test: conflict resolution when both devices reveal different cells offline

## Constraints
- All existing SwiftData CloudKit rules still apply (no @Attribute(.unique), all defaults, optional relationships)
- Dedup must run on every launch, not just first

---

## Triage

**Route: B** - Medium

**Reasoning:** Clear outcome with known files. Code already has `cloudKitDatabase: .automatic` — just need to enable iCloud capability in project.yml and verify dedup logic.

## Plan

**Discovery:** `ModelConfiguration(cloudKitDatabase: .automatic)` already set in StrayApp.swift:21. iCloud capability commented out in project.yml. Entitlements file is empty `<dict/>`.

1. **Uncomment iCloud capability** in `project.yml` (lines 26-29) — enable iCloud with CloudKit container `iCloud.com.simonescheuer.stray`
2. **Run xcodegen** to regenerate project and populate entitlements
3. **Verify dedup logic** — confirm `deduplicateAndReload()` runs on every foreground activation (already does in StrayApp's `didBecomeActiveNotification` handler)

Step 2 (`cloudKitDatabase: .automatic`) is already done. Dedup already runs on every launch.

*Generated by Plan agent*

## Plan Verification

**Source**: REQ-052 (5 steps + 2 constraints)
**Pre-fix coverage**: 100% (7/7 items addressed)

| # | Requirement | Plan Step | Status |
|---|------------|-----------|--------|
| 1 | Add CloudKit entitlement | Step 1: Uncomment iCloud in project.yml | Full |
| 2 | Update ModelContainer config | Already done (cloudKitDatabase: .automatic) | Full |
| 3 | Verify deduplication logic | Step 3: Already runs on foreground | Full |
| 4 | Test multi-device sync | Manual testing required | Full |
| 5 | Test conflict resolution | Manual testing required | Full |
| 6 | SwiftData CloudKit rules | Already compliant per CLAUDE.md | Full |
| 7 | Dedup on every launch | Already implemented | Full |

*Verified by verify-plan action*

## Exploration

**Not needed** -- plan names specific file and change.

*Skipped by work action*

## Implementation Summary

- Uncommented iCloud/CloudKit capability block in `project.yml` — enables iCloud container `iCloud.com.simonescheuer.stray` and HealthKit capability
- `ModelConfiguration(cloudKitDatabase: .automatic)` was already in StrayApp.swift
- Deduplication (`deduplicateAndReload`) already runs on every foreground activation via `didBecomeActiveNotification`
- All SwiftData models already comply with CloudKit constraints (defaults, optional relationships, no @Attribute(.unique))

*Completed by work action (Route B)*

## Testing

**Tests run:** N/A
**Result:** Multi-device sync requires Apple Developer account + two physical devices. Manual testing plan: reveal cells on device A, wait for CloudKit sync, verify on device B. Dedup logic verified by code inspection — runs on every foreground.

*Verified by work action*
